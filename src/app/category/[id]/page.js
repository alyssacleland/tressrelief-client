import React from 'react';

export default function CategoryDetailsPage() {
  return <div>here we will loop thru the services for a given category on service card component admin only: there will be a btn to add a new service, and the cards will have edit/delete on them click on a service card to go to service</div>;
}
